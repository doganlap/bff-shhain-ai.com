# 08 — Multi‑Tenancy & RBAC
- Default: Row‑Level Security (tenant_id everywhere).
- BFF injects tenant context; no client‑controlled tenant.
- RBAC: role → permission mapping stored centrally; UI guards enforced.
