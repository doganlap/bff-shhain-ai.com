name: Security
on:
  pull_request:
  schedule:
    - cron: '0 3 * * *'
jobs:
  scans:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dependency scan (placeholder)
        run: echo "Run npm audit / pip-audit"
      - name: Container scan (placeholder)
        run: echo "Run trivy image scan"
